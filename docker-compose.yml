version: '3.5'
services:
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx:ro
    depends_on:
      - webapp
    ports:
      - "80:80"
  webapp:
    image: ${registry}:${git_commit_id}
    hostname: nginxApp
    expose:
      - "80"

networks:
  default:
    driver: bridge
    name: webapp